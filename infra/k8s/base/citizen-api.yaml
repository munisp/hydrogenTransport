apiVersion: apps/v1
kind: Deployment
metadata:
  name: citizen-api
  namespace: h2fleet
  labels:
    app: citizen-api
    app.kubernetes.io/part-of: h2fleet
spec:
  replicas: 2
  selector:
    matchLabels:
      app: citizen-api
  template:
    metadata:
      labels:
        app: citizen-api
    spec:
      containers:
        - name: citizen-api
          # SECURITY_AUDIT F13: never deploy a mutable :latest tag.
          # :REPLACE_ME is a deliberate placeholder — overlays pin the tag
          # via kustomize `images:` (dev overlay uses :dev); release pipelines
          # must set an immutable tag/digest before applying base directly.
          image: ghcr.io/munisp/h2fleet/citizen-api:REPLACE_ME
          ports:
            - containerPort: 8083
          envFrom:
            - configMapRef:
                name: h2fleet-config
            - secretRef:
                name: h2fleet-secrets
          env:
            - name: PORT
              value: "8083"
          readinessProbe:
            httpGet: { path: /healthz, port: 8083 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /healthz, port: 8083 }
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits: { cpu: 500m, memory: 512Mi }
---
apiVersion: v1
kind: Service
metadata:
  name: citizen-api
  namespace: h2fleet
spec:
  selector:
    app: citizen-api
  ports:
    - name: http
      port: 8083
      targetPort: 8083
