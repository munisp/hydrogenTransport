apiVersion: apps/v1
kind: Deployment
metadata:
  name: carbon-analytics
  namespace: h2fleet
  labels:
    app: carbon-analytics
    app.kubernetes.io/part-of: h2fleet
spec:
  replicas: 1
  selector:
    matchLabels:
      app: carbon-analytics
  template:
    metadata:
      labels:
        app: carbon-analytics
    spec:
      containers:
        - name: carbon-analytics
          # SECURITY_AUDIT F13: never deploy a mutable :latest tag.
          # :REPLACE_ME is a deliberate placeholder — overlays pin the tag
          # via kustomize `images:` (dev overlay uses :dev); release pipelines
          # must set an immutable tag/digest before applying base directly.
          image: ghcr.io/munisp/h2fleet/carbon-analytics:REPLACE_ME
          ports:
            - containerPort: 8094
          envFrom:
            - configMapRef:
                name: h2fleet-config
            - secretRef:
                name: h2fleet-secrets
          env:
            - name: PORT
              value: "8094"
          readinessProbe:
            httpGet: { path: /healthz, port: 8094 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /healthz, port: 8094 }
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests: { cpu: 200m, memory: 512Mi }
            limits: { cpu: 1, memory: 1Gi }
---
apiVersion: v1
kind: Service
metadata:
  name: carbon-analytics
  namespace: h2fleet
spec:
  selector:
    app: carbon-analytics
  ports:
    - name: http
      port: 8094
      targetPort: 8094
