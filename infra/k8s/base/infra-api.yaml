apiVersion: apps/v1
kind: Deployment
metadata:
  name: infra-api
  namespace: h2fleet
  labels:
    app: infra-api
    app.kubernetes.io/part-of: h2fleet
spec:
  replicas: 1
  selector:
    matchLabels:
      app: infra-api
  template:
    metadata:
      labels:
        app: infra-api
    spec:
      containers:
        - name: infra-api
          image: ghcr.io/munisp/h2fleet/infra-api:latest
          ports:
            - containerPort: 8082
          envFrom:
            - configMapRef:
                name: h2fleet-config
            - secretRef:
                name: h2fleet-secrets
          env:
            - name: PORT
              value: "8082"
          readinessProbe:
            httpGet: { path: /healthz, port: 8082 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /healthz, port: 8082 }
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits: { cpu: 500m, memory: 512Mi }
---
apiVersion: v1
kind: Service
metadata:
  name: infra-api
  namespace: h2fleet
spec:
  selector:
    app: infra-api
  ports:
    - name: http
      port: 8082
      targetPort: 8082
