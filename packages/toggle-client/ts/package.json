{
  "name": "@h2fleet/toggle-client",
  "version": "0.1.0",
  "description": "H2Fleet feature-toggle client SDK (TypeScript). Contract per SPEC §3.2: isEnabled(module) -> boolean, 5s local cache, fail-closed.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "5.4.5",
    "vitest": "3.0.5"
  },
  "overrides": {
    "esbuild": "0.25.0"
  }
}
